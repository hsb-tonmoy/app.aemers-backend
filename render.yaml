databases:
  - name: appaemers
    databaseName: appaemers
    user: appaemers_user

services:
  - type: web
    name: appaemers
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn core.wsgi --log-file -"
